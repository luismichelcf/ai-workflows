{
  "name": "ai-workflows",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Deterministic stage engine for coding agents: configurable pipelines whose transitions are verified by code, not by prompt.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "vitest run",
    "test:github": "vitest run --config vitest.github.config.ts",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "files": [
    "dist",
    "schema",
    "templates",
    "action.yml"
  ],
  "types": "./dist/index.d.ts",
  "dependencies": {
    "yaml": "^2.9.1"
  },
  "bin": {
    "ai-workflows": "./dist/bin.js"
  }
}
